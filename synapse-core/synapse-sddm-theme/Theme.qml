import QtQuick 2.15

// Top-level theme surface — prefer using `components/Theme.qml` tokens
QtObject {
    // alias to components Theme for consistency
    // Replace or extend this file to expose top-level tokens
}
